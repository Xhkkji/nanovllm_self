# 20260722 大厂对齐规划

## 1. 当前项目阶段判断

当前 `nanovllm_self` 可以定义为：

**离线批量 PD 原型已经跑通，正在进入在线化前的工程收口阶段。**

已经具备的能力：

- `Paged KV cache / block manager`
- `FlashAttention prefill / decode`
- `chunked prefill`
- `continuous batching` 雏形
- `prefix cache` 与长 prefix 测试
- `CUDA Graph` benchmark 与 bucket 思路
- `PD disaggregation` 原型
- `KVConnector`
- `dict / shared_memory KV backend`
- `PD vs monolithic` 对拍
- 结构化 benchmark 和结果整理

当前还不是完整在线服务：

```text
run_prefill(texts)
-> run_decode(payloads)
-> 一次性返回结果
```

真实在线 serving 更接近：

```text
请求不断进入
scheduler 每轮调度
prefill/decode step 持续推进
token 持续 stream 出去
请求完成后释放资源
```

因此下一阶段重点不应该继续零散补小算子，而是把项目推进到“可展示的在线推理系统”。

## 2. 阿里云 / 阿里 AI Infra 对齐重点

公开岗位和团队资料体现出的重点：

- 推理架构设计
- 全链路服务编排
- 模型推理性能优化
- GPU kernel / FlashAttention / CUDA Graph / TensorRT-LLM / vLLM 相关经验
- 量化、低精度、模型压缩
- 大模型、多模态模型部署
- 性能 profiling 和 benchmark
- 工程稳定性与服务化能力

对项目的启发：

- 需要从 demo 推进到 service。
- 需要能讲清楚从请求进入到 token 输出的完整链路。
- 需要有系统 benchmark 证明优化有效。
- 需要补齐 profiling、metrics、资源释放和异常处理。

建议优先级：

1. 在线服务化 + OpenAI API
2. 全链路 benchmark / profile
3. FlashAttention / CUDA Graph / kernel-level 解释
4. 量化和低精度
5. 多模态推理链路设计
6. 多卡并行设计

## 3. 字节 Seed / AI Infra 对齐重点

公开岗位和团队资料体现出的重点：

- LLM Infra
- 推理服务调度
- vLLM / SGLang / ORCA / TensorRT-LLM
- continuous batching
- KV cache 管理
- prefix cache / chunked prefill
- PD disaggregation
- GPU / HPC / 分布式通信
- MoE / DeepSeek 等新模型结构
- 大规模在线服务稳定性

对项目的启发：

- 需要把 scheduler、PD、KV handoff 做成系统能力，而不是单次脚本。
- 需要支持持续接请求、持续 decode、持续 streaming。
- 需要明确资源生命周期和取消逻辑。
- 需要能对齐 vLLM、SGLang、Mooncake 的核心设计。

建议优先级：

1. continuous batching + online scheduler
2. PD 分离 worker 化
3. KV cache transfer / shared memory / Mooncake 风格设计
4. SGLang/vLLM 对齐能力：prefix cache、chunked prefill、speculative decoding
5. MoE / DeepSeek / 大模型结构
6. HPC / CUDA / 分布式通信基础

## 4. P0：在线化之前最该补的内容

### 4.1 请求生命周期统一

当前 `seq_idx` 和 `request_id=f"req-{seq_idx}"` 还比较临时。

建议引入 `RequestState`，保存：

- `request_id`
- `prompt`
- `input_ids`
- `output_ids`
- `status`
- `sampling_params`
- `created_time`
- `finished_time`
- `error`
- `handoff_payload`
- `stream_queue` 或输出缓存

推荐状态机：

```text
WAITING_PREFILL
PREFILLING
WAITING_DECODE
DECODING
FINISHED
ABORTED
ERROR
```

原因：

- `Sequence` 是模型执行对象。
- `RequestState` 是服务端生命周期对象。
- 取消、超时、错误、流式输出都应该挂在 request 层。

### 4.2 PDCoordinator 改成 step 驱动

当前是阻塞式：

```text
run_prefill()
-> run_decode()
-> return outputs
```

在线化后应变成：

```text
submit_request()
while running:
    prefill_engine.step()
    collect handoff payloads
    decode_engine.restore_payloads(payloads)
    decode_engine.step()
    collect new tokens
    update RequestState
    stream token to client
```

你现在已经有：

- `PrefillEngine.step()`
- `DecodeEngine.step()`

所以基础已经具备，下一步主要是 coordinator 层把两个 step 串成在线 loop。

### 4.3 资源释放和异常清理

在线服务最怕泄漏。

需要明确以下资源什么时候释放：

- prefill block
- decode block
- transfer meta
- shared memory
- scheduler waiting / running 队列中的 seq
- request state

建议补统一接口：

```text
abort_request(request_id)
finish_request(request_id)
cleanup_payload(payload)
cleanup_seq(seq)
cleanup_kv_transfer(transfer_meta)
```

关键场景：

- 请求正常完成
- 请求超时
- 用户取消
- prefill 失败
- KV handoff 失败
- decode restore 失败
- decode 中途失败

### 4.4 输出流式化

agent 和在线 API 基本都需要 streaming token。

当前是最终返回：

```python
return self.decode_engine.run_decode(payloads)
```

在线化后应该支持每步产出事件：

```text
request_id
token_id
token_text
finish_reason
usage
```

内部事件可以先设计成：

```python
@dataclass
class TokenEvent:
    request_id: str
    token_id: int | None
    text: str
    finished: bool
    finish_reason: str | None = None
```

### 4.5 调度器边界明确

当前 scheduler 已经是推荐方向：

```text
1. decode first
2. running prefill chunk
3. waiting new prefill
```

在线化前还需要明确：

- `schedule()` 空返回时 engine loop 如何处理
- block 不够时是 wait、preempt 还是 reject
- waiting 队列如何动态加入请求
- abort 时如何从 waiting/running 清理
- `max_num_batched_tokens` 如何影响 TTFT / ITL
- `prefill_chunk_size` 如何影响长 prompt 和短请求公平性

### 4.6 系统化测试

当前已有 evaluation 偏离线正确性。

在线化前建议补：

- 单请求 submit -> prefill -> decode -> finish
- 多请求 staggered arrival
- 长 prompt + 短 prompt 混合
- `max_tokens=1`，prefill 侧直接 finished
- 请求 cancel 后 block 是否释放
- shared_memory handoff 后是否 unlink
- decode restore 失败时是否释放 local blocks
- 多 batch PD 输出顺序是否按 request_id 保持

## 5. P1：面试加分功能

### 5.1 量化和低精度

建议最小实现方向：

- KV cache dtype 切换：fp16 / bf16 / fp32
- 设计 fp8 / int8 KV cache mock
- 做 accuracy drift 对拍
- 做 memory saving benchmark

需要能讲清楚：

- weight quantization
- activation quantization
- KV cache quantization
- per-channel scale
- per-token scale
- 量化对吞吐、显存和精度的影响

### 5.2 Speculative Decoding

可以先做最小原型：

- n-gram proposer 或 suffix proposer
- target model verify
- acceptance 统计
- benchmark ITL 是否下降

重点不是一开始接完整 draft model，而是能讲清楚：

- proposal
- verification
- acceptance
- lossless guarantee
- 什么时候收益明显
- 什么时候收益不明显

### 5.3 多 GPU / Tensor Parallel

当前 `tensor_parallel_size` 基本只是参数。

建议至少补设计文档或最小样例：

- qkv projection 怎么切
- o projection 怎么切
- MLP gate/up/down 怎么切
- all-reduce 在哪里发生
- KV cache 是否按 TP rank 分片
- scheduler 如何感知多卡资源

### 5.4 DeepSeek / MoE / MLA

热门岗位经常问 DeepSeek、MoE、MLA。

建议补一份设计文档或 toy prototype：

- dense FFN vs MoE FFN
- top-k expert routing
- expert parallel
- load balance
- dispatch/combine
- MLA 如何降低 KV cache
- MoE decode 为什么通信复杂
- 支持 DeepSeek 类模型需要改哪些模块

### 5.5 推荐推进顺序

第五章不要四个方向同时做，优先围绕当前项目已经具备的 `Paged KV cache / PD handoff / online coordinator` 继续推进。

推荐顺序：

1. 先做 `KV cache dtype` 可配置：`fp32 / fp16 / bf16`
2. 补低精度对拍：token drift、logits drift、HF/self 对拍
3. 补 KV memory benchmark：block 数、显存估计、decode tok/s、ITL
4. 再做 `int8 / fp8 KV cache mock`：先验证接口和误差，不追求第一版性能
5. 做离线 speculative decoding 原型：`n-gram proposer + target verify`
6. 多 GPU / Tensor Parallel 先写设计文档或 toy prototype
7. DeepSeek / MoE / MLA 先写设计文档或 toy MoE FFN

当前最适合优先落地的是 `5.1 KV cache 低精度 / 量化`。它和已有 KV cache、shared memory handoff、PD 对拍、benchmark 链路贴合度最高，也最容易形成可展示结果。

## 6. Shared Memory Backend 的定位

当前 shared_memory backend 是合理原型，但不是最终高性能传输方案。

当前链路：

```text
prefill GPU KV cache
-> detach().cpu().clone()
-> numpy
-> shared_memory
-> decode attach shared memory
-> torch.from_numpy(...).clone()
-> copy 到 decode GPU KV cache
```

它优化的是：

- 不经过磁盘
- 可以跨进程共享 CPU 内存
- payload 只携带轻量 metadata

它没有优化掉：

- GPU -> CPU
- CPU -> GPU
- CPU 内存复制

后续如果继续对齐 Mooncake/vLLM，需要考虑：

- CUDA IPC
- NVLink
- GPU Direct RDMA
- NIXL
- Mooncake Store 类似的数据通道
- KV transfer 和 decode compute overlap

## 7. 如果目标是接入 Agent，需要做到什么程度

最小可用能力：

1. 稳定在线请求 API
2. token streaming
3. request_id 生命周期管理
4. cancel / timeout
5. 多轮 chat template
6. stop strings / eos / max_tokens
7. 并发请求 continuous batching
8. 输出格式接近 OpenAI API

推荐内部接口：

```python
request_id = engine.submit(prompt, sampling_params)

for event in engine.stream(request_id):
    print(event.text, end="")
```

推荐服务接口：

```text
POST /v1/completions
POST /v1/chat/completions
```

streaming 输出尽量靠近 OpenAI SSE 风格：

```json
{
  "id": "...",
  "choices": [
    {
      "delta": {"content": "..."},
      "finish_reason": null
    }
  ]
}
```

## 8. 推荐项目补强路线

如果只选最值得做的 5 件事：

1. 做 `OnlinePDCoordinator`
2. 接一个最小 OpenAI-compatible streaming API
3. 做 request lifecycle + cancel + cleanup
4. 做 PD/shared_memory benchmark matrix
5. 补一份系统设计文档：对齐 vLLM/SGLang/Mooncake

完成后，项目表达可以从：

```text
我实现了一个 nano-vLLM 推理 demo
```

升级为：

```text
我实现了一个具备 continuous batching、chunked prefill、prefix cache、PD disaggregation、KV handoff、streaming serving 和结构化 benchmark 的 LLM inference prototype。
```

## 9. 参考资料

- 阿里 ROCK / AI Infra 团队：https://alibaba.github.io/ROCK/zh-Hans/careers/
- 阿里大语言模型推理性能优化岗位：https://www.nowcoder.com/jobs/detail/348627
- 字节 Seed 招聘：https://seed.bytedance.com/zh/career
- 字节 Top Seed：https://seed.bytedance.com/zh/topseed
- vLLM docs：https://docs.vllm.ai/en/stable/
- vLLM automatic prefix caching：https://docs.vllm.ai/en/stable/features/automatic_prefix_caching/
- vLLM disaggregated prefill example：https://docs.vllm.ai/en/v0.20.1/examples/online_serving/disaggregated_prefill/
- SGLang docs：https://docs.sglang.com.cn/
- SGLang speculative decoding：https://docs.sglang.ai/advanced_features/speculative_decoding.html
- Mooncake docs：https://kvcache-ai.github.io/Mooncake/index.html
- Mooncake x vLLM：https://kvcache-ai.github.io/Mooncake/getting_started/examples/vllm-integration/index.html

## 10. Agent 场景驱动的推理系统优化规划

在当前 `nanovllm_self` 的基础上，可以进一步封装一个最小 Agent 系统，再利用 Agent 场景中天然出现的长上下文、重复 prefix、tool schema、RAG 文档和 mixed-length batch，反向驱动推理系统优化。

这个方向适合把项目从“LLM 推理引擎原型”升级成：

```text
基于自研 nanovllm 推理引擎实现 Agent Serving 原型，
针对 Agent 长上下文、多轮对话、工具调用场景优化 TTFT、ITL 和 KV cache 使用效率。
```

### 10.1 为什么 Agent 适合驱动推理优化

Agent 场景天然会产生以下问题：

- 多轮对话上下文越来越长
- system prompt / tool schema / history 有大量重复 prefix
- RAG 会塞入长文档
- tool calling 会产生较长 JSON / observation
- 多 agent 或并发 agent 会制造 mixed-length batch
- 用户取消、超时、重试很常见
- streaming token 是基本需求

这些问题正好对应 AI Infra 推理系统中的核心优化点：

- prefix cache
- chunked prefill
- continuous batching
- long context KV cache 压力
- KV cache eviction
- PD disaggregation
- request scheduling
- streaming serving
- context compression
- memory-aware admission control

### 10.2 推荐项目结构

可以在现有推理系统外新增一层 `agent_self`：

```text
agent_self/
  __init__.py
  session.py
  tools.py
  memory.py
  runtime.py
  prompts.py
  benchmark_agent_long_context.py

nanovllm/
  engine/
  layers/
  models/

pd_self/
  coordinator.py
  prefill_engine.py
  decode_engine.py
  kv_connector.py
  kv_store.py
```

职责划分：

```text
agent_self
-> 负责 Agent runtime、prompt 组织、tool calling、memory、长上下文 workload

nanovllm / pd_self
-> 负责推理引擎、scheduler、KV cache、prefix cache、PD handoff、benchmark
```

### 10.3 Agent 第一版能力

第一版 Agent 不需要复杂，重点是产生真实长上下文负载。

建议支持：

1. `ChatSession`
2. `ToolRegistry`
3. 简单 tool calling
4. `MemoryManager`
5. streaming 预留接口
6. 使用当前 `PDCoordinator.generate()` 作为 backend

第一版调用链：

```text
User
-> AgentSession
-> 拼接 system prompt + history + tool schema
-> nanovllm_self generate
-> 如果识别到 tool call
-> 调用工具
-> tool result 加入 history
-> 再次 generate
```

可先实现的工具：

- `calculator`
- `search_mock`
- `read_file`
- `knowledge_lookup`
- `python_eval_mock`

### 10.4 Agent 暴露的推理瓶颈和对应优化

#### 10.4.1 重复 prefix 导致 TTFT 高

Agent 每轮输入通常是：

```text
system prompt
+ tool schema
+ 历史对话
+ 当前用户问题
```

其中 system prompt、tool schema、部分 history 高度重复。

对应优化：

- prefix cache 命中
- block-level prefix reuse
- prefix cache hit rate 统计
- agent session 级 cache

推荐 benchmark：

```text
同一个 Agent 连续 10 轮对话
对比 prefix cache on/off
指标：TTFT、prefill tokens、cache hit tokens、prefix hit rate
```

#### 10.4.2 长 tool schema 反复 prefill

很多 Agent 框架每次请求都会带完整 tool schema。

对应优化：

- system prompt / tool schema 单独作为 shared prefix
- 对 tool schema 做 prefix prefill
- 多请求共享同一份 prefix KV

可以新增概念：

```text
AgentPrefixCache
```

逻辑上把下面内容作为固定 prefix：

```text
system_prompt + tool_schema
```

提前写入 KV cache，后续每轮复用。

#### 10.4.3 RAG 文档导致 prompt 很长

RAG Agent 会把多个文档塞进上下文。

问题：

- prefill 阶段很重
- decode 被长 prefill 阻塞
- 短请求 ITL 被拉高
- batch 内 mixed-length 更明显

对应优化：

- chunked prefill
- decode-first scheduling
- long prompt 分块
- `prefill_chunk_size` 调参

推荐 benchmark：

```text
batch 中混合：
- 短 chat 请求
- 长 RAG 请求
- 中等 tool 请求

对比：
- chunked_prefill off / on
- decode-first off / on

指标：
- 短请求 ITL
- 长请求 TTFT
- overall throughput
- schedule step token composition
```

#### 10.4.4 多轮历史导致 KV cache 压力

长对话会持续增长：

```text
turn1 + turn2 + ... + turnN
```

问题：

- KV cache block 占用越来越高
- block manager 容易满
- 新请求 admission 失败
- decode append 失败

对应优化：

- KV cache usage metrics
- request-level block accounting
- session memory truncation
- sliding window
- summary compression
- cache eviction 策略

第一版可以先做简单策略：

```text
超过 max_context_tokens 后：
保留 system prompt
保留最近 N 轮
旧 history 压缩成 summary
```

#### 10.4.5 Agent 需要 streaming 和 cancel

真实 Agent 用户会中断生成，或者 tool call 需要提前停止。

对应优化：

- request lifecycle
- `abort_request()`
- block cleanup
- shared_memory cleanup
- stream token event
- stop strings
- timeout

这会直接推动在线化能力落地。

### 10.5 Agent 与在线推理接口

第一阶段可以先使用离线接口：

```python
outputs = pd.generate(
    texts=[prompt],
    max_tokens=128,
    temperature=0.0,
    ignore_eos=True,
)
```

在线化后应升级为：

```python
request_id = engine.submit(prompt, sampling_params)

for event in engine.stream(request_id):
    print(event.text, end="")
```

内部 token event 可以设计为：

```python
@dataclass
class TokenEvent:
    request_id: str
    token_id: int | None
    text: str
    finished: bool
    finish_reason: str | None = None
```

如果要接 OpenAI 风格 API，streaming 输出可以对齐：

```json
{
  "id": "...",
  "choices": [
    {
      "delta": {"content": "..."},
      "finish_reason": null
    }
  ]
}
```

### 10.6 Agent Long Context Benchmark

建议新增：

```text
agent_self/benchmark_agent_long_context.py
```

覆盖三类负载：

1. 多轮对话 repeated prefix
2. tool schema repeated prefix
3. RAG long document mixed with short chat

对比维度：

- prefix cache on/off
- chunked prefill on/off
- PD on/off
- dict backend vs shared_memory backend
- short / medium / long prompt
- equal length / mixed length

指标：

- TTFT
- ITL / TPOT
- E2E latency
- prefill throughput
- decode throughput
- generated tokens/s
- KV blocks used
- prefix cache hit tokens
- prefix cache hit rate
- KV transfer latency
- queue time

### 10.7 推荐推进顺序

建议按以下顺序推进：

1. 做 `AgentSession` 离线版，直接调用当前 `PDCoordinator.generate()`
2. 加 `agent_self/benchmark_agent_long_context.py`
3. 统计 TTFT / prefill tokens / generated tokens / KV blocks
4. 对比 prefix cache 命中和不命中
5. 开启 chunked prefill，测 mixed workload
6. 做 `OnlinePDCoordinator`
7. 支持 streaming token
8. 支持 cancel / timeout / cleanup
9. 做 Agent streaming API
10. 再考虑 KV eviction / context compression

### 10.8 面试表达方式

可以这样描述项目升级方向：

```text
我不是单纯实现一个 LLM 推理 demo，而是用 Agent 场景反向驱动推理系统优化。
Agent 场景会产生长上下文、重复 prefix、tool schema、RAG 文档和 mixed-length batch。
我基于这些 workload 实现并验证 prefix cache、chunked prefill、decode-first scheduling、PD disaggregation 和 KV handoff。
```

这个方向能同时对齐：

- 阿里关注的全链路推理服务编排
- 字节关注的大规模 serving / scheduler / KV cache
- vLLM 的 continuous batching / prefix cache / OpenAI serving
- SGLang 的 Agent workload / prefix reuse / structured generation
- Mooncake 的 PD disaggregation / KV transfer / long-context serving
