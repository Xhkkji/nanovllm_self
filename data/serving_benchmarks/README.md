# Serving Benchmark Datasets

This folder stores local datasets for LLM serving / inference benchmark work.

## Files

### `synthetic_serving_lengths.jsonl`

Synthetic request-length matrix generated locally.

Purpose:

- controlled input/output length benchmark
- TTFT / ITL / throughput smoke tests
- PD handoff latency tests
- short/long prompt mixed workload tests

Schema:

```json
{
  "id": "synth-0000",
  "profile": "short_in_short_out",
  "prompt": "...",
  "input_len_hint": 32,
  "output_len": 16
}
```

Current size:

```text
200 requests
```

### `synthetic_serving_qwen3_tokenized.jsonl`

Tokenized synthetic request set for `/home/xhk/model/Qwen3-0.6B/`.

Purpose:

- use real tokenizer lengths instead of the rough `input_len_hint`
- provide benchmark-ready `input_tokens`, `max_tokens`, and `total_tokens`
- filter requests by context budget before running PD / serving tests

Additional fields:

```json
{
  "model_path": "/home/xhk/model/Qwen3-0.6B/",
  "tokenizer": "qwen3",
  "input_tokens": 72,
  "max_tokens": 15,
  "total_tokens": 87,
  "fits_512": true,
  "fits_2048": true
}
```

Generated by:

```bash
/home/xhk/miniconda3/envs/pytorch/bin/python scripts/prepare_synthetic_serving_dataset.py --sample-per-profile 5
```

Current size:

```text
200 requests
```

Related files:

```text
synthetic_serving_qwen3_summary.json
synthetic_serving_qwen3_tokenized_sample.jsonl
```

### `ShareGPT_V3_unfiltered_cleaned_split.json`

Full ShareGPT cleaned split.

Source:

```text
ModelScope mirror of ShareGPT_Vicuna_unfiltered
https://modelscope.cn/api/v1/datasets/otavia/ShareGPT_Vicuna_unfiltered/repo?Revision=master&FilePath=ShareGPT_V3_unfiltered_cleaned_split.json
```

Notes:

- Hugging Face direct download may be unstable in this environment.
- This file is large, about 642 MB.
- Raw schema is a list of conversations:

```json
{
  "id": "...",
  "conversations": [
    {"from": "human", "value": "..."},
    {"from": "gpt", "value": "..."}
  ]
}
```

Current size:

```text
94145 conversations
```

### `sharegpt_prompts_1k.jsonl`

Lightweight processed subset from ShareGPT.

Purpose:

- quick serving benchmark
- realistic chat-prompt length distribution
- first pass before reading the full 642 MB raw JSON

Schema:

```json
{
  "id": "...",
  "source": "ShareGPT_V3_unfiltered_cleaned_split",
  "prompt": "...",
  "output_len": 64,
  "num_conversations": 12
}
```

Current size:

```text
1000 requests
```

## Recommended Order

1. Start with `synthetic_serving_qwen3_tokenized.jsonl` for controlled sequence length with real Qwen3 token counts.
2. Use `sharegpt_prompts_1k.jsonl` for a quick realistic chat workload.
3. Use the full `ShareGPT_V3_unfiltered_cleaned_split.json` only when the benchmark path is stable.
